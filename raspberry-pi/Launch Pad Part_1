# type: ignore 

import time



for x in range(10, 0, -1):       # Using a [for] loop to express the values between 0-10 and making it count down by [-1]
    time.sleep(0.5000)           # Adds a delay in between the countdown
    print(x)                     # Prints all the values going from [10] down to [0] by the increment amount (-1)
    time.sleep(0.5000)
print("liftoff")                 # Prints to confirm the launch and end of program.
